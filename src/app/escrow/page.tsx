import EscrowFeature from "@/components/escrow/escrow-feature";

// Very simple. Leave this as a server component and pull in a client component and render it.
export default function Page() {
  return <EscrowFeature />;
}
